name: Commit
description: Generate commit message
---
<rules>
    <rule>
        Generate a commit following "Conventional commits" specifications with exhaustive description using the following context.
    </rule>
    <rule>
        Do not add the context at the end of the commit message
    </rule>
</rules>
<context>
    @Git Diff
</context>
